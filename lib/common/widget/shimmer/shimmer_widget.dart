import 'package:flutter/material.dart';

class ShimmerWidget extends StatelessWidget {
  const ShimmerWidget({super.key, required double shimmerWidth, required double shimmerHieght, required int shimmerRadius});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
